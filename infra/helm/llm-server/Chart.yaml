apiVersion: v2
name: llm-server
description: Multi-model LLM serving platform — per-model resources generated from the config/models.json registry.
type: application
version: 0.1.0
appVersion: "1.0.0"
