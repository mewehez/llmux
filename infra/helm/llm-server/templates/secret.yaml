apiVersion: v1
kind: Secret
metadata:
  name: llm-secrets
  labels:
    {{- include "llm-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Not truly sensitive in kind, but demonstrates the secretKeyRef pattern.
  REDIS_URL: redis://redis:6379/0
